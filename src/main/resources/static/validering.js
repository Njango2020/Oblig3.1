/*
function validerNavn(navn){
    const regexp = /^[a-zA-ZæøåÆØÅ. \-]{2,20};
    const ok = regexp.test(navn);
    if (!ok){
        $("#feilFornavn").html("Navnet må fylles inn");
        return false;
    }
    else{
        $("#feilFornavn").html("");
        return true;
    }
}

function validerEtterNavn(etternavn){
    const regexp = /^[a-zA-ZæøåÆØÅ. \-]{2,20};
    const ok = regexp.test(etternavn);
    if (!ok){
        $("#feilEtternavn").html("må fylles inn");
        return false;
    }
    else{
        $("#feilEtternavn").html("");
        return true;
    }
}

function validerEpost(epost){
    const regexp = /^[a-zA-ZæøåÆØÅ. \-]{2,20};
    const ok = regexp.test(epost);
    if (!ok){
        $("#feilEpost").html("må fylles");
        return false;
    }
    else{
        $("#feilEpost").html("");
        return true;
    }
}

function validerTlf(telefonnr){
    const regexp = /^[a-zA-ZæøåÆØÅ. \-]{2,20};
    const ok = regexp.test(telefonnr);
    if (!ok){
        $("#feilTelefonnr").html("Navnet må fylles inn");
        return false;
    }
    else{
        $("#feilTelefonnr").html("");
        return true;
    }
}
function validerAntall(antall){
    const regexp = /^[a-zA-ZæøåÆØÅ. \-]{1,20};
    const ok = regexp.test(antall);
    if (!ok){
        $("#feilAntall").html("må fylles inn");
        return false;
    }
    else{
        $("#feilAntall").html("");
        return true;
    }
}

 */
